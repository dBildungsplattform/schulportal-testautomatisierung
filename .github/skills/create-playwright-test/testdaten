SPSH-2512 — Testdaten-Plan (Arbeitsdatei)

Ziel
- Landesadmin (ENV-Credentials) anmelden
- Kachel "Schulportal-Administration" prüfen + zur Administration navigieren
- Abmelden

Szenarien
| Szenario | Entitäten | Konstellation | Cleanup |
|---|---|---|---|
| Landesadmin meldet sich an, navigiert zur Admin und meldet sich ab | Landesadmin (process.env.USER / process.env.PW) | Positivfall | keiner |

API-Funktionen (beforeEach)
- freshLoginPage(page) → loginPage  (bereits im bestehenden beforeEach vorhanden)
- header = new HeaderPage(page)     (bereits im bestehenden beforeEach vorhanden)

Zieldatei
- tests/Authentifizierung.spec.ts — erster test.describe-Block
- Tests "Erfolgreicher Login" und "Erfolgreicher Logout" werden durch den neuen Test ersetzt

afterEach Cleanup
- keiner (keine Testdaten erstellt)
